import React from "react";


const TextField = ({} ) => {
  
  return (
          <input
            type="text"
            data-testid="avocado-textfield"
          />
        )
};

export default TextField;